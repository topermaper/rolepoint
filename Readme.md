This is just an exercise for Rolepoint.

It is available in my AWS server:
http://18.130.8.148/rolepoint/rolepoint.html

When user clicks on search button it basically makes an ajax call to the backend where a python script reads a JSON file containing the data, uses the regexp provided to find matching items and returns the results to the front end.